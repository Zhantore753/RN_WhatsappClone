import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container:{
        backgroundColor: '#1EC81B',
        position: "absolute",
        width: 55,
        height: 55,
        borderRadius: 50,
        justifyContent: 'center',
        alignItems: 'center',
        bottom: 15,
        right: 15
    },
});

export default styles;